;;; config.el -*- lexical-binding: t; -*-
;;
;; PIMacs Docker testing configuration
;; Minimal config - PIMacs modules handle most settings

;; User info (optional)
(setq user-full-name "PIMacs Tester"
      user-mail-address "pimacs@example.com"
      doom-theme nil ;; I use a slightly customized Zenburn theme by PIMacs.
      )

;; Local variables:
;; coding: utf-8
;; End:
