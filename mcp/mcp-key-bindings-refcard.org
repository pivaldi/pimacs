#+title: PIMacs/mcp Key Bindings with Keyboard Option "azerty"

This refcard is auto-generated by [[https://github.com/pivaldi/pimacs][PIMacs]].

* PIMacs Added Key Bindings


** Keymap =mcp-hub-mode-map=
Keymap for ‘mcp-hub-mode’.


* Default Mode Key Bindings


** Keymap =mcp-hub-mode-map=
Keymap for ‘mcp-hub-mode’.

- =<DEL>= calls =scroll-down-command= : scroll text of selected window down ARG lines; or near full screen if no ARG.
- =<SPC>= : <leader>
- =<TAB>= calls =forward-button= : move to the Nth next button, or Nth previous button if N is negative.
- =+= calls =mcp-hub-add-root= : add a root directory to the currently selected MCP server.
- =-= calls =mcp-hub-remove-root= : remove a root directory from the currently selected MCP server.
- =0= calls =digit-argument= : part of the numeric argument for the next command.
- =1= calls =digit-argument= : part of the numeric argument for the next command.
- =2= calls =digit-argument= : part of the numeric argument for the next command.
- =3= calls =digit-argument= : part of the numeric argument for the next command.
- =4= calls =digit-argument= : part of the numeric argument for the next command.
- =5= calls =digit-argument= : part of the numeric argument for the next command.
- =6= calls =digit-argument= : part of the numeric argument for the next command.
- =7= calls =digit-argument= : part of the numeric argument for the next command.
- =8= calls =digit-argument= : part of the numeric argument for the next command.
- =9= calls =digit-argument= : part of the numeric argument for the next command.
- =<= calls =beginning-of-buffer= : move point to the beginning of the buffer.
- === calls =mcp-hub-view-roots= : view the roots for the currently selected MCP server.
- =>= calls =end-of-buffer= : move point to the end of the buffer.
- =?= calls =describe-mode= : display documentation of current major mode and minor modes.
- =d= calls =mcp-hub-show-server-detail= : view the detail info for the currently selected MCP server.
- =g= calls =revert-buffer= : replace current buffer text with the text of the visited file on disk.
- =h= calls =describe-mode= : display documentation of current major mode and minor modes.
- =k= calls =mcp-hub-close-server= : stop the currently selected MCP server.
- =K= calls =mcp-hub-close-all-server= : stop all running MCP servers.
- =l= calls =mcp-hub-view-log= : view the event log for the currently selected MCP server.
- =n= calls =next-line= : move cursor vertically down ARG lines.
- =p= calls =previous-line= : move cursor vertically up ARG lines.
- =q= calls =quit-window= : quit WINDOW and bury its buffer.
- =r= calls =mcp-hub-restart-server= : restart the currently selected MCP server.
- =R= calls =mcp-hub-restart-all-server= : restart all configured MCP servers.
- =s= calls =mcp-hub-start-server= : start the currently selected MCP server.
- =S= calls =mcp-hub-start-all-server= : start all configured MCP servers.
- ={= calls =tabulated-list-narrow-current-column= : narrow the current tabulated list column by N chars.
- =}= calls =tabulated-list-widen-current-column= : widen the current tabulated-list column by N chars.
- =C-M-i= calls =backward-button= : move to the Nth previous button, or Nth next button if N is negative.
- =M-<left>= calls =tabulated-list-previous-column= : go to the start of the column point is in on the current line.
- =M-<right>= calls =tabulated-list-next-column= : go to the start of the next column after point on the current line.
- =S-<SPC>= calls =scroll-down-command= : scroll text of selected window down ARG lines; or near full screen if no ARG.
- =<backtab>= calls =backward-button= : move to the Nth previous button, or Nth next button if N is negative.
- =<follow>-<link>= : mouse-face
