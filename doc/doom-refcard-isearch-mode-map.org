#+title: Key Bindings of Keymap =isearch-mode-map=

This reafcard is auto-generated by [[https://github.com/pivaldi/pimacs][PIMacs]].
* Table of Content :TOC_2:
- [[#keymap-isearch-mode-map][Keymap =isearch-mode-map=]]
  - [[#prefix-esc-calls-isearch-cancel--terminate-the-search-and-go-back-to-the-starting-point][Prefix =<ESC>= calls =isearch-cancel= : terminate the search and go back to the starting point.]]
  - [[#prefix-f1--prefix][Prefix =<f1>= : +prefix]]
  - [[#prefix-c-h--prefix][Prefix =C-h= : +prefix]]
  - [[#prefix-c-x--prefix][Prefix =C-x= : +prefix]]
  - [[#prefix-m-s--prefix][Prefix =M-s= : +prefix]]
  - [[#prefix-help--prefix][Prefix =<help>= : +prefix]]

* Keymap =isearch-mode-map=
- =<DEL>= calls =isearch-delete-char= : undo last input item during a search.
- =<RET>= calls =isearch-exit= : exit search normally.
- =<SPC>..~= calls =isearch-printing-char= : add this ordinary printing CHAR to the search string and search.
- =<TAB>..C-j= calls =isearch-printing-char= : add this ordinary printing CHAR to the search string and search.
- =C-\= calls =isearch-toggle-input-method= : toggle input method in interactive search.
- =C-^= calls =isearch-toggle-specified-input-method= : select an input method and turn it on in interactive search.
- =C-a= : Jump to one of the current isearch candidates. #pim
- =C-g= calls =isearch-abort= : abort incremental search mode if searching is successful, signaling quit.
- =C-q= calls =isearch-quote-char= : quote special characters for incremental search.
- =C-r= calls =isearch-repeat-backward= : repeat incremental search backwards.
- =C-s= calls =isearch-repeat-forward= : repeat incremental search forwards.
- =C-w= calls =isearch-yank-word-or-char= : pull next character or word from buffer into search string.
- =C-y= calls =isearch-yank-kill= : pull string from kill ring into search string.
- =C-M-%= calls =isearch-query-replace-regexp= : start `query-replace-regexp' with string to replace from last search string.
- =C-M-d= calls =isearch-del-char= : delete character from end of search string and search again.
- =C-M-i= calls =isearch-complete= : complete the search string from the strings on the search ring.
- =C-M-r= calls =isearch-repeat-backward= : repeat incremental search backwards.
- =C-M-s= calls =isearch-repeat-forward= : repeat incremental search forwards.
- =C-M-w= calls =isearch-yank-symbol-or-char= : pull next character or symbol from buffer into search string.
- =C-M-y= calls =isearch-yank-char= : pull next character from buffer into search string.
- =C-M-z= calls =isearch-yank-until-char= : pull everything until next instance of CHAR from buffer into search string.
- =M-%= calls =isearch-query-replace= : start `query-replace' with string to replace from last search string.
- =M-c= calls =isearch-toggle-case-fold= : toggle case-fold searching on or off.
- =M-e= calls =isearch-edit-string= : edit the search string in the minibuffer.
- =M-n= calls =isearch-ring-advance= : advance to the next search string in the ring.
- =M-p= calls =isearch-ring-retreat= : retreat to the previous search string in the ring.
- =M-r= calls =isearch-toggle-regexp= : toggle regexp searching on or off.
- =M-y= calls =isearch-yank-pop-only= : replace just-yanked search string with previously killed string.
- =S-<SPC>= calls =isearch-printing-char= : add this ordinary printing CHAR to the search string and search.
- =<backspace>= calls =undefined= : beep to tell the user this binding is undefined.
- =<return>= calls =isearch-exit= : exit search normally.
- =<xterm>-<paste>= calls =isearch-xterm-paste= : pull terminal paste into search string.
- =Â€..ÿ= calls =isearch-printing-char= : add this ordinary printing CHAR to the search string and search.
** Prefix =<ESC>= calls =isearch-cancel= : terminate the search and go back to the starting point.
- =<ESC> C-M-%= calls =isearch-query-replace-regexp= : start `query-replace-regexp' with string to replace from last search string.
- =<ESC> C-M-d= calls =isearch-del-char= : delete character from end of search string and search again.
- =<ESC> C-M-i= calls =isearch-complete= : complete the search string from the strings on the search ring.
- =<ESC> C-M-r= calls =isearch-repeat-backward= : repeat incremental search backwards.
- =<ESC> C-M-s= calls =isearch-repeat-forward= : repeat incremental search forwards.
- =<ESC> C-M-w= calls =isearch-yank-symbol-or-char= : pull next character or symbol from buffer into search string.
- =<ESC> C-M-y= calls =isearch-yank-char= : pull next character from buffer into search string.
- =<ESC> C-M-z= calls =isearch-yank-until-char= : pull everything until next instance of CHAR from buffer into search string.
- =<ESC> M-%= calls =isearch-query-replace= : start `query-replace' with string to replace from last search string.
- =<ESC> M-c= calls =isearch-toggle-case-fold= : toggle case-fold searching on or off.
- =<ESC> M-e= calls =isearch-edit-string= : edit the search string in the minibuffer.
- =<ESC> M-n= calls =isearch-ring-advance= : advance to the next search string in the ring.
- =<ESC> M-p= calls =isearch-ring-retreat= : retreat to the previous search string in the ring.
- =<ESC> M-r= calls =isearch-toggle-regexp= : toggle regexp searching on or off.
- =<ESC> M-s= : +prefix
- =<ESC> M-y= calls =isearch-yank-pop-only= : replace just-yanked search string with previously killed string.
*** Prefix =<ESC> <ESC>= calls =isearch-cancel= : terminate the search and go back to the starting point.
- =<ESC> <ESC> <ESC>= calls =isearch-cancel= : terminate the search and go back to the starting point.
** Prefix =<f1>= : +prefix
- =<f1> ?= calls =isearch-help-for-help= : display Isearch help menu.
- =<f1> b= : bindings
- =<f1> k= calls =isearch-describe-key= : display documentation of the function invoked by isearch key.
- =<f1> m= calls =isearch-describe-mode= : display documentation of Isearch mode.
- =<f1> q= calls =help-quit= : just exit from the Help command's command loop.
- =<f1> <f1>= calls =isearch-help-for-help= : display Isearch help menu.
- =<f1> C-h= calls =isearch-help-for-help= : display Isearch help menu.
- =<f1> <help>= calls =isearch-help-for-help= : display Isearch help menu.
** Prefix =C-h= : +prefix
- =C-h ?= calls =isearch-help-for-help= : display Isearch help menu.
- =C-h b= : bindings
- =C-h k= calls =isearch-describe-key= : display documentation of the function invoked by isearch key.
- =C-h m= calls =isearch-describe-mode= : display documentation of Isearch mode.
- =C-h q= calls =help-quit= : just exit from the Help command's command loop.
- =C-h <f1>= calls =isearch-help-for-help= : display Isearch help menu.
- =C-h C-h= calls =isearch-help-for-help= : display Isearch help menu.
- =C-h <help>= calls =isearch-help-for-help= : display Isearch help menu.
** Prefix =C-x= : +prefix
- =C-x \= calls =isearch-transient-input-method= : activate transient input method in interactive search.
*** Prefix =C-x 8= : +prefix
- =C-x 8 <RET>= calls =isearch-char-by-name= : read a character by its Unicode name and add it to the search string.
**** Prefix =C-x 8 e= : +prefix
- =C-x 8 e <RET>= calls =isearch-emoji-by-name= : read an Emoji name and add it to the search string COUNT times.
** Prefix =M-s= : +prefix
- =M-s <SPC>= calls =isearch-toggle-lax-whitespace= : toggle lax-whitespace searching on or off.
- =M-s '= calls =isearch-toggle-char-fold= : toggle char-fold searching on or off.
- =M-s _= calls =isearch-toggle-symbol= : toggle symbol searching on or off.
- =M-s c= calls =isearch-toggle-case-fold= : toggle case-fold searching on or off.
- =M-s e= calls =isearch-edit-string= : edit the search string in the minibuffer.
- =M-s i= calls =isearch-toggle-invisible= : toggle invisible searching on or off.
- =M-s o= calls =isearch-occur= : run `occur' using the last search string as the regexp.
- =M-s r= calls =isearch-toggle-regexp= : toggle regexp searching on or off.
- =M-s w= calls =isearch-toggle-word= : toggle word searching on or off.
- =M-s C-e= calls =isearch-yank-line= : pull rest of line from buffer into search string.
- =M-s M-<= calls =isearch-beginning-of-buffer= : go to the first occurrence of the current search string.
- =M-s M->= calls =isearch-end-of-buffer= : go to the last occurrence of the current search string.
*** Prefix =M-s h= : +prefix
- =M-s h l= calls =isearch-highlight-lines-matching-regexp= : exit Isearch mode and call `highlight-lines-matching-regexp'.
- =M-s h r= calls =isearch-highlight-regexp= : exit Isearch mode and call `highlight-regexp'.
** Prefix =<help>= : +prefix
- =<help> ?= calls =isearch-help-for-help= : display Isearch help menu.
- =<help> b= : bindings
- =<help> k= calls =isearch-describe-key= : display documentation of the function invoked by isearch key.
- =<help> m= calls =isearch-describe-mode= : display documentation of Isearch mode.
- =<help> q= calls =help-quit= : just exit from the Help command's command loop.
- =<help> <f1>= calls =isearch-help-for-help= : display Isearch help menu.
- =<help> C-h= calls =isearch-help-for-help= : display Isearch help menu.
- =<help> <help>= calls =isearch-help-for-help= : display Isearch help menu.
