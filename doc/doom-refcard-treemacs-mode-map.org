#+title: Key Bindings of Keymap =treemacs-mode-map=

This reafcard is auto-generated by [[https://github.com/pivaldi/pimacs][PIMacs]].
* Table of Content :TOC_2:
- [[#keymap-treemacs-mode-map][Keymap =treemacs-mode-map=]]
  - [[#prefix-c--prefix][Prefix =c= : +prefix]]
  - [[#prefix-o--prefix][Prefix =o= : +prefix]]
  - [[#prefix-t--prefix][Prefix =t= : +prefix]]
  - [[#prefix-y--prefix][Prefix =y= : +prefix]]
  - [[#prefix-c-c--prefix][Prefix =C-c= : +prefix]]

* Keymap =treemacs-mode-map=
Keymap for ‘treemacs-mode’.

- =<DEL>= calls =scroll-down-command= : scroll text of selected window down ARG lines; or near full screen if no ARG.
- =<RET>= calls =treemacs-RET-action= : run the appropriate RET action for the current button.
- =<SPC>= : <leader>
- =<TAB>= calls =treemacs-TAB-action= : run the appropriate TAB action for the current node.
- =!= calls =treemacs-run-shell-command-for-current-node= : run a shell command on the current node.
- =-= calls =negative-argument= : begin a negative numeric argument for the next command.
- =0= calls =digit-argument= : part of the numeric argument for the next command.
- =1= calls =digit-argument= : part of the numeric argument for the next command.
- =2= calls =digit-argument= : part of the numeric argument for the next command.
- =3= calls =digit-argument= : part of the numeric argument for the next command.
- =4= calls =digit-argument= : part of the numeric argument for the next command.
- =5= calls =digit-argument= : part of the numeric argument for the next command.
- =6= calls =digit-argument= : part of the numeric argument for the next command.
- =7= calls =digit-argument= : part of the numeric argument for the next command.
- =8= calls =digit-argument= : part of the numeric argument for the next command.
- =9= calls =digit-argument= : part of the numeric argument for the next command.
- =<= calls =treemacs-decrease-width= : decrease the value for `treemacs-width' with `treemacs-width-increment'.
- === calls =treemacs-fit-window-width= : make treemacs wide enough to display its entire content.
- =>= calls =treemacs-increase-width= : increase the value for `treemacs-width' with `treemacs-width-increment'.
- =?= calls =treemacs-common-helpful-hydra= : summon a helpful hydra to show you the treemacs keymap.
- =b= calls =treemacs-add-bookmark= : add the current node to Emacs' list of bookmarks.
- =C= calls =treemacs-cleanup-litter= : collapse all nodes matching any of `treemacs-litter-directories'.
- =d= calls =treemacs-delete-file= : delete node at point.
- =g= calls =treemacs-refresh= : refresh the project at point.
- =h= calls =treemacs-COLLAPSE-action= : run the appropriate COLLAPSE action for the current button.
- =H= calls =treemacs-collapse-parent-node= : close the parent of the node at point.
- =l= calls =treemacs-RET-action= : run the appropriate RET action for the current button.
- =m= calls =treemacs-move-file= : move file (or directory) at point.
- =n= calls =treemacs-next-line= : go to next line.
- =p= calls =treemacs-previous-line= : go to previous line.
- =P= calls =treemacs-peek-mode= : minor mode that allows you to peek at buffers before deciding to open them.
- =q= calls =treemacs-quit= : quit treemacs with `bury-buffer'.
- =Q= calls =treemacs-kill-buffer= : kill the treemacs buffer.
- =r= calls =treemacs-refresh= : refresh the project at point.
- =R= calls =treemacs-rename-file= : rename the file/directory at point.
- =s= calls =treemacs-resort= : select a new permanent value for `treemacs-sorting' and refresh.
- =u= calls =treemacs-goto-parent-node= : select parent of selected node, if possible.
- =w= calls =treemacs-set-width= : select a new value for `treemacs-width'.
- =W= calls =treemacs-extra-wide-toggle= : expand the treemacs window to an extr-wide state (or turn it back).
- =C-?= calls =treemacs-advanced-helpful-hydra= : summon a helpful hydra to show you the treemacs keymap.
- =C-j= calls =treemacs-next-project= : move to the next project root node.
- =C-k= calls =treemacs-previous-project= : move to the next project root node.
- =M-!= calls =treemacs-run-shell-command-in-project-root= : run an asynchronous shell command in the root of the current project.
- =M-h= calls =treemacs-COLLAPSE-action= : run the appropriate COLLAPSE action for the current button.
- =M-H= calls =treemacs-root-up= : move treemacs' root one level upward.
- =M-l= calls =treemacs-RET-action= : run the appropriate RET action for the current button.
- =M-L= calls =treemacs-root-down= : move treemacs' root into the directory at point.
- =M-m= calls =treemacs-bulk-file-actions= : activate the bulk file actions hydra.
- =M-n= calls =treemacs-next-neighbour= : select next node at the same depth as currently selected node, if possible.
- =M-N= calls =treemacs-next-line-other-window= : scroll forward COUNT lines in `next-window'.
- =M-p= calls =treemacs-previous-neighbour= : select previous node at the same depth as currently selected node, if possible.
- =M-P= calls =treemacs-previous-line-other-window= : scroll backward COUNT lines in `next-window'.
- =M-<down>= calls =treemacs-move-project-down= : switch position of the project at point and the one below it.
- =M-<up>= calls =treemacs-move-project-up= : switch position of the project at point and the one above it.
- =S-<SPC>= calls =scroll-down-command= : scroll text of selected window down ARG lines; or near full screen if no ARG.
- =<backtab>= calls =treemacs-collapse-all-projects= : collapses all projects.
- =<next>= calls =treemacs-next-page-other-window= : scroll forward COUNT pages in `next-window'.
- =<prior>= calls =treemacs-previous-page-other-window= : scroll backward COUNT pages in `next-window'.
- =<return>= calls =treemacs-RET-action= : run the appropriate RET action for the current button.
- =<tab>= calls =treemacs-TAB-action= : run the appropriate TAB action for the current node.
** Prefix =c= : +prefix
- =c d= calls =treemacs-create-dir= : create a new directory.
- =c f= calls =treemacs-create-file= : create a new file.
** Prefix =o= : +prefix
- =o c= calls =treemacs-visit-node-close-treemacs= : open current node without and close treemacs.
- =o h= calls =treemacs-visit-node-horizontal-split= : open current file or tag by horizontally splitting `next-window'.
- =o o= calls =treemacs-visit-node-no-split= : open current node without performing any window split or window selection.
- =o r= calls =treemacs-visit-node-in-most-recently-used-window= : open current file or tag in window selected by `get-mru-window'.
- =o v= calls =treemacs-visit-node-vertical-split= : open current file or tag by vertically splitting `next-window'.
- =o x= calls =treemacs-visit-node-in-external-application= : open current file according to its mime type in an external application.
*** Prefix =o a= : +prefix
- =o a a= calls =treemacs-visit-node-ace= : open current file or tag in window selected by `ace-window'.
- =o a h= calls =treemacs-visit-node-ace-horizontal-split= : open current file by horizontally splitting window selected by `ace-window'.
- =o a v= calls =treemacs-visit-node-ace-vertical-split= : open current file by vertically splitting window selected by `ace-window'.
** Prefix =t= : +prefix
- =t a= calls =treemacs-filewatch-mode= : minor mode to let treemacs auto-refresh itself on file system changes.
- =t c= calls =treemacs-indicate-top-scroll-mode= : minor mode which shows whether treemacs is scrolled all the way to the top.
- =t d= calls =treemacs-git-commit-diff-mode= : minor mode to display commit differences for your git-tracked projects.
- =t f= calls =treemacs-follow-mode= : toggle `treemacs-follow-mode'.
- =t g= calls =treemacs-git-mode= : toggle `treemacs-git-mode'.
- =t h= calls =treemacs-toggle-show-dotfiles= : toggle the hiding and displaying of dotfiles.
- =t i= calls =treemacs-hide-gitignored-files-mode= : toggle `treemacs-hide-gitignored-files-mode'.
- =t n= calls =treemacs-indent-guide-mode= : toggle `treemacs-indent-guide-mode'.
- =t v= calls =treemacs-fringe-indicator-mode= : toggle `treemacs-fringe-indicator-mode'.
- =t w= calls =treemacs-toggle-fixed-width= : toggle whether the local treemacs buffer should have a fixed width.
** Prefix =y= : +prefix
- =y a= calls =treemacs-copy-absolute-path-at-point= : copy the absolute path of the node at point.
- =y f= calls =treemacs-copy-file= : copy file (or directory) at point.
- =y n= calls =treemacs-copy-filename-at-point= : copy the filename of the node at point.
- =y p= calls =treemacs-copy-project-path-at-point= : copy the absolute path of the current treemacs root.
- =y r= calls =treemacs-copy-relative-path-at-point= : copy the path of the node at point relative to the project root.
- =y v= calls =treemacs-paste-dir-at-point-to-minibuffer= : paste the directory at point into the minibuffer.
** Prefix =C-c= : +prefix
*** Prefix =C-c C-p= : +prefix
- =C-c C-p a= calls =treemacs-add-project-to-workspace= : add a project at given PATH to the current workspace.
- =C-c C-p d= calls =treemacs-remove-project-from-workspace= : remove the project at point from the current workspace.
- =C-c C-p p= calls =treemacs-projectile= : add one of `projectile-known-projects' to the treemacs workspace.
- =C-c C-p r= calls =treemacs-rename-project= : give the project at point a new name.
**** Prefix =C-c C-p c= : +prefix
- =C-c C-p c a= calls =treemacs-collapse-all-projects= : collapses all projects.
- =C-c C-p c c= calls =treemacs-collapse-project= : close the project at point.
- =C-c C-p c o= calls =treemacs-collapse-other-projects= : collapses all projects except the project at point.
*** Prefix =C-c C-w= : +prefix
- =C-c C-w a= calls =treemacs-create-workspace= : create a new workspace.
- =C-c C-w d= calls =treemacs-remove-workspace= : delete a workspace.
- =C-c C-w e= calls =treemacs-edit-workspaces= : edit your treemacs workspaces and projects as an `org-mode' file.
- =C-c C-w f= calls =treemacs-set-fallback-workspace= : set the current workspace as the default fallback.
- =C-c C-w n= calls =treemacs-next-workspace= : switch to the next workspace.
- =C-c C-w r= calls =treemacs-rename-workspace= : select a workspace to rename.
- =C-c C-w s= calls =treemacs-switch-workspace= : select a different workspace for treemacs.
