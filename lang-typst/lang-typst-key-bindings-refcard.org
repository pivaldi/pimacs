#+title: PIMacs/lang-typst Key Bindings with Keyboard Option "azerty"

This refcard is auto-generated by [[https://github.com/pivaldi/pimacs][PIMacs]].

* PIMacs Added Key Bindings


** Keymap =typst-ts-mode-map=
Keymap for [[help:typst-ts-mode][typst-ts-mode]].


* Default Mode Key Bindings


** Keymap =typst-ts-mode-map=
Keymap for [[help:typst-ts-mode][typst-ts-mode]].

- =<RET>= calls =typst-ts-editing-return= : rET behavior depending context.
- =<TAB>= calls =typst-ts-editing-cycle= : cycle.
- =M-<down>= calls =typst-ts-editing-meta-down= : see [[help:typst-ts-editing-meta--dwim][typst-ts-editing-meta--dwim]].
- =M-<left>= calls =typst-ts-editing-meta-left= : see [[help:typst-ts-editing-meta--dwim][typst-ts-editing-meta--dwim]].
- =M-<return>= calls =typst-ts-editing-meta-return= : depending on context, insert a heading or insert an item.
- =M-<right>= calls =typst-ts-editing-meta-right= : see [[help:typst-ts-editing-meta--dwim][typst-ts-editing-meta--dwim]].
- =M-<up>= calls =typst-ts-editing-meta-up= : see [[help:typst-ts-editing-meta--dwim][typst-ts-editing-meta--dwim]].
*** Prefix =C-c= : +prefix
- =C-c <RET>= calls =typst-ts-main-file-ask= : ask for Typst main file and persist it as a file-local variable.
- =C-c '= calls =typst-ts-edit-indirect= : edit the block at point with [[help:edit-indirect-region][edit-indirect-region]].
- =C-c C-c= : Compile Php file or lint it if prefixed. #pim
- =C-c C-o= calls =typst-ts-mc-open-at-point= : follow thing at point.
- =C-c C-p= calls =typst-ts-preview= : preview the typst document output.
- =C-c C-w= calls =typst-ts-watch-mode= : watch(hot compile) current typst file.
- =C-c C-S-c= calls =typst-ts-compile-and-preview= : compile & Preview.
